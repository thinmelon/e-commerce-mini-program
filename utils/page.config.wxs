var swipeConfig = {
    // 是否显示面板指示点
    indicatorDots: true,
    // 是否自动切换
    autoplay: true,
    // 是否采用衔接滑动
    circular: true,
    // 自动切换时间间隔
    interval: 5000,
    // 滑动动画时长
    duration: 1000
};

module.exports = {
    swipe: swipeConfig
}